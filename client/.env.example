# Leave empty for local development (Vite proxies /api → localhost:3001 automatically).
# In production, set this to your deployed backend URL including /api:
#   e.g. https://your-backend.railway.app/api
VITE_API_URL=
